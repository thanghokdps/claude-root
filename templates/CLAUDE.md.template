# {{PROJECT_NAME}} — Claude Code Guide

> Generated by harness init on {{INIT_DATE}}. Fill in the sections marked with `<!-- ... -->`.

## What this project is

<!-- 2–3 sentences: what it does, who uses it, key tech choices -->

## Run the project

```bash
<!-- Fill in: install, dev, test, reset commands -->
```

## Architecture

```
<!-- Fill in: top-level data flow diagram -->
```

## Key directories

| Path | Purpose |
|------|---------|
| `src/` | Main source |

## Stack

**{{STACK_LIST}}**

## Available skills

| Skill | Command | Purpose |
|-------|---------|---------|
| feature | `/feature <description>` | Risk intake → plan → build → review |
| fix-bug | `/fix-bug <symptom>` | Root cause → fix → verify → commit |
| code-review | `/code-review [low\|medium\|high]` | Review diff for bugs + cleanups |
| checkpoint | `/checkpoint` | Progress vs plan + quality gates |
| sync-memory | `/sync-memory` | Pull latest + rebuild memory from your commits |

## Hooks (auto-registered)

| Hook | Trigger | Does |
|------|---------|------|
| `commit-quality-gate.sh` | Before `git commit` | Block secrets, debug artifacts |
| `branch-guard.sh` | Before `git push` | Warn on push to main/master |
| `save-commit-memory.sh` | After `git commit` | Save commit to `.claude/memory/` |
| `state-breadcrumb.sh` | Session end | Append session summary to memory |
| `scope-gate.sh` | Each prompt | Flag high-risk patterns, stale memory |

## Constraints

<!-- Add project-specific constraints here -->
<!-- Examples: -->
<!-- - TypeScript strict — npx tsc --noEmit before any commit -->
<!-- - pnpm only — never use npm or yarn -->
<!-- - No direct DB calls in route handlers — use service layer -->

## Environment variables

```env
<!-- Fill in required env vars -->
```
